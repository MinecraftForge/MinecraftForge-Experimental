--- a/net/minecraft/data/registries/RegistriesDatapackGenerator.java
+++ b/net/minecraft/data/registries/RegistriesDatapackGenerator.java
@@ -21,6 +_,11 @@
     private final String name;
     private final Collection<RegistryDataLoader.RegistryData<?>> registryData;
     private final CompletableFuture<HolderLookup.Provider> registries;
+    private final java.util.function.Predicate<ResourceKey<?>> entryPredicate;
+
+    public static net.minecraftforge.common.data.RegistryDataBuilder builder() {
+        return net.minecraftforge.common.data.RegistryDataBuilder.of();
+    }
 
     public RegistriesDatapackGenerator(
         final PackOutput output,
@@ -28,14 +_,30 @@
         final Collection<RegistryDataLoader.RegistryData<?>> registryData,
         final CompletableFuture<HolderLookup.Provider> registryContents
     ) {
+        this(output, name, registryData, registryContents, _ -> true);
+    }
+
+    public RegistriesDatapackGenerator(
+        final PackOutput output,
+        final String name,
+        final Collection<RegistryDataLoader.RegistryData<?>> registryData,
+        final CompletableFuture<HolderLookup.Provider> registryContents,
+        final java.util.function.Predicate<ResourceKey<?>> entryPredicate
+    ) {
         this.name = name;
         this.registryData = registryData;
         this.registries = registryContents;
         this.output = output;
+        this.entryPredicate = entryPredicate;
+    }
+
+    /** Forge: Call this to get the registry holder lookup provider that includes elements added via {@link net.minecraftforge.common.data.RegistryDataBuilder} */
+    public CompletableFuture<HolderLookup.Provider> getRegistryProvider() {
+        return this.registries;
     }
 
     public static RegistriesDatapackGenerator forWorldLayer(final PackOutput output, final CompletableFuture<HolderLookup.Provider> registryContents) {
-        return new RegistriesDatapackGenerator(output, "world", RegistryDataLoader.WORLD_REGISTRIES, registryContents);
+        return new RegistriesDatapackGenerator(output, "world", RegistryDataLoader.getWorldAndDimensionRegistries(), registryContents);
     }
 
     public static DataProvider forReloadableLayer(final PackOutput output, final CompletableFuture<HolderLookup.Provider> registryContents) {
@@ -68,11 +_,16 @@
                     PackOutput.PathProvider pathProvider = this.output.createRegistryElementsPathProvider(registryKey);
                     return CompletableFuture.allOf(
                         registry.listElements()
+                            .filter(holder -> shouldDump(holder.key()))
                             .<CompletableFuture<?>>map(e -> dumpValue(pathProvider.json(e.key().identifier()), cache, writeOps, v.elementCodec(), e.value()))
                             .toArray(CompletableFuture[]::new)
                     );
                 }
             );
+    }
+
+    protected boolean shouldDump(ResourceKey<?> key) {
+        return this.entryPredicate.test(key);
     }
 
     private static <E> CompletableFuture<?> dumpValue(
