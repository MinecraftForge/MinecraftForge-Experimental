--- a/net/minecraft/data/loot/BlockLootSubProvider.java
+++ b/net/minecraft/data/loot/BlockLootSubProvider.java
@@ -707,12 +_,16 @@
 
     protected abstract void generate();
 
+    protected Iterable<Block> getKnownBlocks() {
+        return BuiltInRegistries.BLOCK.stream().filter(b -> net.minecraft.resources.Identifier.DEFAULT_NAMESPACE.equals(BuiltInRegistries.BLOCK.getKey(b).getNamespace()))::iterator;
+    }
+
     @Override
     public void run() {
         this.generate();
         Set<ResourceKey<LootTable>> seen = new HashSet<>();
 
-        for (Block block : BuiltInRegistries.BLOCK) {
+        for (Block block : getKnownBlocks()) {
             if (block.isEnabled(this.enabledFeatures)) {
                 block.getLootTable()
                     .ifPresent(
