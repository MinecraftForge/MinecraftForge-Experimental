--- a/net/minecraft/world/entity/ai/goal/RemoveBlockGoal.java
+++ b/net/minecraft/world/entity/ai/goal/RemoveBlockGoal.java
@@ -34,7 +_,7 @@
 
     @Override
     public boolean canUse() {
-        if (!getServerLevel(this.removerMob).getGameRules().get(GameRules.MOB_GRIEFING)) {
+        if (!net.minecraftforge.event.ForgeEventFactory.getMobGriefingEvent(getServerLevel(this.removerMob.level()), this.removerMob)) {
             return false;
         } else if (this.nextStartTick > 0) {
             this.nextStartTick--;
@@ -142,7 +_,8 @@
         );
         return chunkaccess == null
             ? false
-            : chunkaccess.getBlockState(pos).is(this.blockToRemove)
+            : !chunkaccess.getBlockState(pos).canEntityDestroy(level, pos, this.removerMob) ? false
+                : chunkaccess.getBlockState(pos).is(this.blockToRemove)
                 && chunkaccess.getBlockState(pos.above()).isAir()
                 && chunkaccess.getBlockState(pos.above(2)).isAir();
     }
