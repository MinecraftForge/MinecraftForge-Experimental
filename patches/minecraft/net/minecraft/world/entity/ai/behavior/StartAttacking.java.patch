--- a/net/minecraft/world/entity/ai/behavior/StartAttacking.java
+++ b/net/minecraft/world/entity/ai/behavior/StartAttacking.java
@@ -30,7 +_,11 @@
                             if (!body.canAttack(livingentity)) {
                                 return false;
                             } else {
-                                attackTarget.set(livingentity);
+                                var changeTargetEvent = net.minecraftforge.event.ForgeEventFactory.onLivingChangeTargetBehavior(body, livingentity);
+                                if (changeTargetEvent == null)
+                                    return false;
+
+                                attackTarget.set(changeTargetEvent.getNewTarget());
                                 cantReachSince.erase();
                                 return true;
                             }
